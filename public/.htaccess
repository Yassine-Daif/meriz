# Repli SPA pour Apache : toute adresse inconnue renvoie l'application
# (l'équivalent du try_files nginx). Copié dans dist/ par le build.
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.html [L]
