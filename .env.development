# Réglages de `npm run dev` (fichier commité, aucune donnée secrète).
# Origine du serveur Meriz API en local, sans /api : lancez-le avec
# `php artisan serve` dans le dépôt meriz-api.
VITE_API_URL=http://127.0.0.1:8000
